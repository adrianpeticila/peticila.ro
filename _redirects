# Cloudflare Pages redirects
# www → apex (Cloudflare Pages handles this automatically via custom domain,
# but explicit rule is a safety net)
https://www.peticila.ro/* https://peticila.ro/:splat 301
